# `mysql` environment config goes here
modules:
    config:
        Db:
          dsn: '%DSN%'
          user: '%DB_USERNAME%'
          password: '%DB_PASSWORD%'
          dump:
            - tests/Support/Data/install-mysql.sql
            - tests/Support/Data/create-user.sql
          populate: true
          cleanup: true
